export const metadata = {
  title: "Compare | Movie DB",
  description: "A simple movie website built with Next.js",
};

export default function CompareLayout({ children }) {
  return <>{children}</>;
}
